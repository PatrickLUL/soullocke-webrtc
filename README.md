# Soullocke WebRTC v4-debug

Neu:
- Oben sichtbar: `v4-debug`
- Jede Kachel zeigt Debug-Werte:
  - socket
  - peer
  - ice
  - track
  - video width/height
  - readyState
  - paused/muted

Deploy:
```bash
git add .
git commit -m "add v4 debug overlay"
git push
```

Nach dem Test bitte Screenshot schicken, besonders von der schwarzen Remote-Kachel.
