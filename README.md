# Soullocke WebRTC v6.1-performance

Fix:
- Debugdaten nutzen echte Zeilenumbrüche statt sichtbares `\\n`.
- Sichtbare Version oben: `v6.1-performance`.

Deploy:
```bash
git add .
git commit -m "fix debug line breaks"
git push
```
